% +CFG Package containing any default configuration parameters
% MATLAB Version 9.7 (R2019b Update 5) 15-Jun-2020
%
% Each function should take a variable number of inputs and returns a
% variable number of outputs. The standard input syntax refers to each
% parameter name, each of which is a struct field of a constant struct that
% holds all relevant parameters (e.g. p.L for parameter 'L'). The number of
% outputs should equal the total number of requested parameter names; or,
% if multiple inputs are requested but only a single output, then the
% output is a struct with those input field names. If no input arguments
% are given and a single output argument is given, then the full `p`
% parameters struct for that file is returned. If no input or output
% arguments are specified, then a list of parameters is printed to the
% Command Window.
%
% <strong>Enumeration Classes<?strong>
%  Hem       - Class with enumerations for Left and Right hemispheres
%  TrialType - Class with enumerations for different Trial Types
%
% <strong>Files</strong>
%  default   - Default parameters for `sol` classes
%  gfx       - Default parameters for functions in tbl.gfx package
%  stats     - Default parameters for functions in tbl.stats package
